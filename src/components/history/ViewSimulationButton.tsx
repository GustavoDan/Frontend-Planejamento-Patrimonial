import { Button } from "../ui/button";

const ViewSimulationButton = () => {
    return (
        <Button className="text-[#B4B4B4] bg-[#363636] rounded-[2.5rem] hover:bg-zinc-800 hover:text-zinc-400">
            Ver no gráfico
        </Button>
    );
};

export default ViewSimulationButton;
